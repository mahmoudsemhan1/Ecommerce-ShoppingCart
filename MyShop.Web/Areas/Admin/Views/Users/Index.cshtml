@model IEnumerable<ApplicationUser>
@{
    ViewBag.PageTitle = "User Managment";
    ViewBag.CardTitle = "View Users";
}
<div class="m-3">
    <a class="btn btn-primary" href="/Identity/Account/Register">Create New Users</a>
</div>

<table class="table">
    <tr>
        <td>Name</td>
        <td>Email</td>
        <td>PhoneNumber</td>
        <td>Actions</td>
    </tr>
    @foreach (var item in Model)
    {
        <tr>
            <td>@item.Name</td>
            <td>@item.Email</td>
            <td>@item.PhoneNumber</td>

            <td>
                <a class="btn btn-success" asp-action="LockUmlock">
                    <i class="fas fa-lock-open"></i>
                </a>
                <a class="btn btn-danger" asp-area="Admin" asp-controller="Category" asp-action="Delete" asp-route-id="@item.Id">
                    Delete
                </a>
            </td>
        </tr>
    }
</table>

